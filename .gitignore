# Rust build output
target/
/build

# Don't check in a local rustup toolchain override
/rust-toolchain.toml

# Local env config
.cargo/

# Editor/JVM stray files
.attach_*
.worktrees/

# Local agent/tooling directories
.agents/
.ralph/

# Loopy stage working directories (contain embedded git repos) + runtime state
.loopy/stages/
.loopy/workspaces/
.loopy/state.json
.loopy/projects/
.loopy/custom-pipeline.json
.loopy/linear-run.json

# Per-project config generated by `loopy init` / the agent loop harness
/loopy.yml
/ralph.yml

# Web frontend dependencies and build cache (committed dist/ is the artifact)
web/node_modules/
web/tsconfig.tsbuildinfo
